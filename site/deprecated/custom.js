//"use strict";
//addEventListener('click', start);

//addEventListener('load', start);
/*
function start() {
  var url = window.location.href;
  var q = url.indexOf("?");
  if (q < 0) return reportNone();
  var query = url.substring(q + 1);
    var defs = query.split("&");
    for (var i=0; i<defs.length; i++) {
       defs[i] = defs[i].split("=");
    }
}


var element = document.getElementById('submitlogin');
element.addEventListener('click', validateForm ());


function reportNone() {
    var list = document.querySelector("#list");
    list.innerHTML = "<li>No parameters found</li>";
}


//  display(defs);

//function validatelogin() {
//window.location = "dashboard.html";
///var url = window.location.href;

//}
*/
/*
function validateForm() {
        var un = document.loginform.LoginEmail1.value;
        var pw = document.loginform.LoginPassword1.value;
        var username = "username";
        var password = "password";
        if ((un == username) && (pw == password)) {
            window.location = "dashboard.html";
            return false;
        }
        else {
            alert ("Login was unsuccessful, please check your username and password");
        }
  }*/


//addEventListener('load', start);

//function start() {
//  window.location.href = "dashboard.html";
  //  var url = window.location.href;
  //  var q = url.indexOf("?");
  //  if (q < 0) return reportNone();
  //  var query = url.substring(q + 1);
//    var defs = query.split("&");
//    for (var i=0; i<defs.length; i++) {
//        defs[i] = defs[i].split("=");
//    }
  //  display(defs);
//}

/*function reportNone() {
    var list = document.querySelector("#list");
    list.innerHTML = "<li>No parameters found</li>";
}

function display(defs) {
    var items = "";
    for (var i=0; i<defs.length; i++) {
        items = items + "<li>" + defs[i][0] + " = " + defs[i][1] + "</li>";
    }
    var list = document.querySelector("#list");
    list.innerHTML = items;
}
*/
